[phases.setup]
nixPkgs = ["python310", "python310Packages.pip", "mesa", "glib", "xorg.libX11"]

[phases.install]
cmds = ["pip3 install -r requirements.txt"]

[start]
cmd = "uvicorn server:app --host 0.0.0.0 --port $PORT"
